# Install Chef Omnibus with an embedded ruby.
# Even though most of that is going to get destroyed by right_image.sh, we need the /opt/chef/bin stubs
curl -L http://www.opscode.com/chef/install.sh | bash


